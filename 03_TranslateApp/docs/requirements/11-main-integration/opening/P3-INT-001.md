# Task P3-INT-001: Luồng Khởi tạo Dịch thuật (Integration Flow)

## Metadata
- **Task ID**: P3-INT-001
- **Priority**: CRITICAL
- **Phase**: 3
- **Estimated Effort**: L
- **Dependencies**: P1-SEG-001, P2-SESS-001, P2-NAV-001, P2-UI-002
- **Status**: opening

## Mô tả

### Mục đích
Kết nối các module rời rạc để tạo thành luồng công việc đầu tiên: Người dùng chọn file -> Hệ thống xử lý và hiển thị lên màn hình.

### Phạm vi
**Làm:**
- [ ] Cập nhật `AppWindow` (hoặc Controller liên quan) để xử lý sự kiện nút "Open File".
- [ ] Tích hợp `DocumentProcessor` (Phase 4 cũ - có thể dùng placeholder nếu chưa xong, hoặc implement cơ bản) để lấy text từ DOCX.
- [ ] Gọi `TextSegmenter` để chia nhỏ văn bản thành các trang.
- [ ] Gọi `SessionManager.create_session` để khởi tạo phiên làm việc mới.
- [ ] Cập nhật `LeftSidebar` bằng hàm `populate_pages`.
- [ ] Cập nhật `CenterPanel` để hiển thị trang 1 (Tiếng Nhật).

### Acceptance Criteria
- [x] AC1: Sau khi chọn file DOCX, Sidebar bên trái tự động hiển thị danh sách trang.
- [x] AC2: Center Panel hiển thị nội dung tiếng Nhật của trang đầu tiên.
- [x] AC3: Tên file được hiển thị đúng trên thanh tiêu đề hoặc Top Bar.

## Technical Notes
- Cần wire-up các callback giữa `LeftSidebar` (on_page_selected) và `AppWindow`.
- Luồng dữ liệu: UI (Button Open) -> AppWindow -> Logic Layer -> UI (Sidebar/Center).

## References
- REQUIREMENTS_VI.md: Section 1.2
