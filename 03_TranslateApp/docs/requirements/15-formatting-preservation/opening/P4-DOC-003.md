# Task P4-DOC-003: Tái cấu trúc file DOCX tiếng Việt (Export with Formatting)

## Metadata
- **Task ID**: P4-DOC-003
- **Priority**: CRITICAL
- **Phase**: 4
- **Estimated Effort**: L
- **Dependencies**: P4-DOC-001, P3-API-002
- **Status**: opening

## Mô tả

### Mục đích
Sử dụng dữ liệu dịch thuật từ Session và thông tin định dạng (Metadata) đã trích xuất ở task P4-DOC-001 để tạo ra file DOCX tiếng Việt hoàn chỉnh, giữ nguyên mọi style (font, màu sắc, bảng biểu) của file gốc.

### Phạm vi
**Làm:**
- [ ] Xây dựng hàm `export_to_docx(session_id, output_path)`.
- [ ] Đọc metadata định dạng từ session.
- [ ] Sử dụng `python-docx` để tạo document mới hoặc clone từ template.
- [ ] Áp dụng style (font, size, bold, italic, color) cho từng đoạn văn bản tiếng Việt.
- [ ] Giữ nguyên cấu trúc bảng và vị trí các dấu 【】 nếu có.

### Acceptance Criteria
- [x] AC1: File DOCX xuất ra có nội dung là tiếng Việt đã dịch.
- [x] AC2: Định dạng (màu sắc, in đậm, font chữ) giống 95% so với file Nhật gốc.
- [x] AC3: File mở được bình thường trên Microsoft Word mà không bị lỗi cấu trúc.

## Technical Notes
- Cần xử lý kỹ việc mapping giữa "Markdown text" trong session và "Run objects" trong DOCX.
- Màu sắc cần được chuyển đổi về định dạng RGB hex mà `python-docx` hiểu được.

## References
- REQUIREMENTS_VI.md: Section 4.1.2
