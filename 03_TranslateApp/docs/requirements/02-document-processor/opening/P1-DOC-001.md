# Task P1-DOC-001: Xử lý chuyển đổi DOCX sang Markdown (và lưu Format)

## Metadata
- **Task ID**: P1-DOC-001
- **Priority**: CRITICAL
- **Phase**: 1
- **Estimated Effort**: M
- **Dependencies**: P1-SETUP-001
- **Status**: opening

## Mô tả

### Mục đích
Đọc file DOCX đầu vào, chuyển đổi nội dung sang Markdown để dễ xử lý dịch thuật, đồng thời phải LƯU LẠI được các định dạng (font, size, color, bold, italic) để sau này tái tạo lại bản dịch tiếng Việt y như bản gốc (Section 4.1).

### Phạm vi
**Làm:**
- [ ] Đọc các đoạn văn (paragraphs) và bảng (tables) từ file DOCX.
- [ ] Trích xuất text tiếng Nhật.
- [ ] Trích xuất metadata định dạng của từng đoạn/run (font name, size, color, bold, italic).
- [ ] Xây dựng cấu trúc dữ liệu lưu trữ metadata này.

**KHÔNG làm:**
- Ghi file DOCX (sẽ làm ở P1-DOC-002)
- Phân đoạn `【text】` (sẽ làm ở P1-SEG-001)

### Acceptance Criteria
- [ ] AC1: Đọc thành công file DOCX và trả về danh sách text tiếng Nhật.
- [ ] AC2: Trả về kèm cấu trúc metadata định dạng (font, màu sắc, kiểu chữ) tương ứng với từng text block.

### Technical Notes
- Hình ảnh bị bỏ qua. Header/Footer có thể bỏ qua.
- Các thuộc tính định dạng cần lưu chi tiết đến cấp độ Run của python-docx.

## Implementation Guide

### Output
- List text (Markdown base)
- Object/Dict chứa format metadata

### Algorithm/Logic
```python
def extract_docx(file_path):
    # Dùng Document(file_path)
    # Lặp qua paragraphs, lấy text
    # Lặp qua runs trong paragraph, lưu metadata...
    pass
```

### Libraries Required
- `python-docx`: Thao tác với DOCX file

### Error Handling
| Lỗi | Xử lý |
|------|-------|
| File DOCX hỏng/không đúng định dạng | Raise error, UI sẽ bắt và báo lỗi |

## Testing Checklist
- [ ] Unit test đọc DOCX thường, DOCX có bảng (tables), DOCX có màu chữ và in đậm.

## References
- REQUIREMENTS_VI.md: Section 4.1.1, 4.1.2
